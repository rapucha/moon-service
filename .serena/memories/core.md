# Moon Service

- `AGENTS.md` is the highest-priority repository authority for agent work. Use `docs/README.md` to find canonical product, architecture, API, and development documents.
- The product is a web-first, anonymous Moon-photography opportunity service. Accounts and an installed client are outside the current MVP.
- Read `mem:backend/core` for the backend boundary, `mem:tech_stack` for the implementation stack, and `mem:conventions` for durable project constraints.
- Read `mem:suggested_commands` and `mem:task_completion` for validation and delivery. Read `mem:java_lsp_safety` before relying on Maven results after Java indexing.
- Follow `mem:memory_maintenance` when the user explicitly asks to add or update project memories.
