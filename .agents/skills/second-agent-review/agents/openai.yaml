interface:
  display_name: "Second-Agent Review"
  short_description: "Independent read-only change review"
  default_prompt: "Use $second-agent-review to run an independent read-only review of the current staged changes."
