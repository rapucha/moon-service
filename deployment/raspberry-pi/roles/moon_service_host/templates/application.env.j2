MOON_LOCATION_RESOLVER=open-meteo
MOON_WEATHER_PROVIDER=open-meteo

# Optional. Leave unset to keep /admin/** disabled. To enable it, generate a
# high-entropy value locally and add MOON_ADMIN_TOKEN=<value> to this host-only
# file. Ansible deliberately does not overwrite this file after creating it.
