interface:
  display_name: "Implementation Scope Review"
  short_description: "Independent pre-implementation scope review"
  default_prompt: "Use $implementation-scope-review to ask a fresh agent to assess this issue and propose an independently mergeable PR sequence before implementation."
