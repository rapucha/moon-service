MOON_LOCATION_RESOLVER=open-meteo
MOON_WEATHER_PROVIDER=open-meteo
MOON_HOSTED_ALPHA_ENABLED=false

# Hosted-alpha publication remains disabled until issue #123. Before that
# separately approved activation window, generate a high-entropy value locally,
# add MOON_ADMIN_TOKEN=<value> to this host-only file, and change the setting
# above to true. Ansible deliberately does not overwrite this file after
# creating it.
